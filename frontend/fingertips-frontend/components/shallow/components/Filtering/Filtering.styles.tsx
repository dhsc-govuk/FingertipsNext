import styled from 'styled-components';

export const SelectWrapper = styled.div({
  display: 'flex',
  flexDirection: 'column',
  gap: '1rem',
  span: {
    fontWeight: 'bold',
  },
  select: {
    width: '100%',
  },
});
